import React from "react";
import styles from "./card_maker.module.css";
const Card_maker = (props) => {
  return (
    <section className={styles.maker}>
      <h1 className={styles.title}>Card Maker</h1>
    </section>
  );
};

export default Card_maker;
