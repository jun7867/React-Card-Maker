import React from "react";
import styles from "./card_preview.module.css";
const Card_preview = (props) => {
  return (
    <section className={styles.preview}>
      <h1 className={styles.title}>Card Preview</h1>
    </section>
  );
};

export default Card_preview;
